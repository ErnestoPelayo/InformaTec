<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.macbook.informatec">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ActivityConferences" />
        <activity android:name=".ui.ActivityContest" />
        <activity
            android:name=".activity_actions"
            android:label="@string/title_activity_actions"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.Activity_ConferencesCreate" />
        <activity android:name=".ui.Activity_ContestCreate" />
        <activity android:name=".ui.Activity_ContestDelete"></activity>
    </application>

</manifest>